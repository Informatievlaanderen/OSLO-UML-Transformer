import { OptionalFetch } from './fetchContext';
/**
 * Use prefix.cc to look up all recorded prefixes
 */
export declare function lookupAllPrefixes(options?: OptionalFetch): Promise<Record<string, string>>;
