{
  "name": "oslo-uml-validator",
  "version": "1.0.0",
  "description": "script to validate all the OSLO models",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build-clean": "tsc --build --clean ../tsconfig.json",
    "start": "npm run build && node ./index.js"
  },
  "engines": {
    "npm": ">=10.0.0",
    "node": ">=20.0.0"
  },
  "author": "Kristof Vandenbroucke",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@oslo-flanders/ea-converter": "^0.0.20-alpha.0",
    "axios": "^1.6.8",
    "extract-zip": "^2.0.1"
  }
}
